# 最终交付合同 v1

2026-08-31，来源：用户要求连续推进到最终版本，不再逐窗口协调或在组件阶段停止。
本窗口为唯一协调入口；托盘浮窗优先推进。网页端现有接口冻结，分类待用户确认，既非完成也非取消。

## MUST验收清单

1. END-001：唯一可见进度入口memory/进度.md，显示当前步骤、已完成、下一步、阻碍；共享对接指向最新记录。
2. END-002：同一交付入口启动浮窗与必要采集组件；不依赖预先手动启动4317服务；重复启动不产生冲突。
3. END-003：两个既有任务真实标识/变化经核对；结构化计划真实取得才显示步骤、下一步。不把列表访问当跨窗口订阅。
4. END-004：任务跳转至少两个不同threadId准确打开原任务；不创建、恢复执行或改变任务内容，不仅打开首页。
5. END-005：托盘呼出、隐藏、关闭后恢复、退出均在真实桌面测试；退出仅退出浮窗及自己启动的组件，不影响Codex任务。
6. END-006：首次失败、断线保留、重连、重启验证；错误不是成功空列表，陈旧内容明显标记。
7. END-007：交付工件与验证版本摘要一致；区分自动测试、真实数据、原生桌面、用户确认和安装；构建不等于安装通过。
8. END-008：用户提供的公司/项目/具体工作/对象/期限才提取；相对时间必须有消息本身的时间依据；不可拿轮次时间替代消息时间。网页/浮窗保持产品边界。

## 执行与停止边界

- 延续TDD。阶段PASS只用于继续工作，不是终止条件。
- 用户当前授权支持原目标内GUI集成、候选包构建与验收；不授权账号登录、凭据复制、任务控制、隐私截图传输、安全绕过或发布。
- 不可获得的官方跨窗口事件和不可用的原生自动化必须标明版本、实测与解除条件；继续完成不受影响的工作，最终只对需要用户授权/操作/选择的事项请求协助。
- 不把受阻目标改写成更小的“完成版本”，也不因无关测试数字增加就宣称实际可用。

## 已知能力实测

- 旧npm codex-cli 0.147.0：既有全轮次读取-32603证据保留。
- 桌面bundled codex-cli 0.150.0-alpha.12.2：本轮thread/read includeTurns成功读取两个指定现有任务；仅输出结构数量，工具5393b9。仍notLoaded、直接plan通知0，不足以证明实时同步。
- Computer Use初始化及reset后重试均apply deny-read ACLs失败；不是产品失败，不以其他UI自动化绕过。
