# 最终交付合同 v2

2026-08-31，用户已接受深色紧凑风格，并要求通过TDD推出最终版。v1保留历史，本版补齐明确的完成门槛，不缩减用户目标。

1. END-001：memory/进度.md为唯一进度入口，准确记录设计已接受、当前步骤与阻碍；对接同步指向它。
2. END-002：同一用户从相同/不同路径并发启动只允许一个新版本持有原子实例占用；第二实例在采集器启动及快捷键注册前退出；异常终止后可重新启动，不依赖删锁文件。旧候选1—8未采用新保护，换版需先退出它们。
3. END-003：两个既有任务的真实变化可核对；真实跨窗口结构化计划变化，经同thread/turn来源绑定及新鲜度验证，正确投影第几步、下一步并回读。只做到“无计划不显示”是安全降级，不能作为此项功能完成。能力不可得时保持partial/受阻；只有用户明确接受缩减目标才可称缩减版最终完成。
4. END-004：两个不同任务ID实际准确打开原任务，不创建/恢复执行/更改任务内容，不只开首页。
5. END-005：真实桌面验证托盘呼出隐藏、关闭恢复、退出及退出不影响Codex任务；原生自动化不可用时由用户在本窗口协助最小人工验收，不绕安全边界。
6. END-006：新交付入口验证首次失败、断线保留/陈旧标记、恢复、重启；错误不冒充成功空表。
7. END-007：同一交付工件与测试源摘要一致，区分构建、浏览器、真实读取、原生和安装；无证据不称最终版。
8. END-008：仅用户提供且来源绑定的公司/项目/对象/期限/分区入业务字段；相对时间使用消息级依据，否则省略。样例状态和时间不能进入生产。网页端边界不动。

原始用户目标中实时步骤、非干扰与原生最后一跳不能因某个组件PASS而取消。任务控制、登录、秘密复制、隐私截图发送、安全绕过和对外发布仍不在授权内。
