# 静态浮窗审美候选

最新确认（2026-08-31）：用户选择A，仅用于托盘任务浮窗；浏览器workspace项目总览为另一用途。见[用户确认与产品边界](decision-a-and-product-boundaries.md)。下文为候选阶段历史记录，不再表示A仍待选择；生产能力尚未验收。

## 方向取舍

- A：纵向分组，优点是领域界限清楚、行距舒适；代价是比B高40px；超过3条或标题很长时需要内部滚动，不能缩小文字塞满。
- B：侧栏领域标记和共享列，优点是少占纵向空间；代价是较紧密；320px或放大文字时改为纵向排列，不能挤压标题。

## 可执行验收清单

两稿分别在目标390x560/420x520与320 CSS px渲染；把根文字放大200%；注入超长中文标题与对象路径；测量页面与任务区域横向溢出和每条标题/下一步是否被裁剪；保留无期限/计划样例；计算正文/次要文字/领域文字对比度不低于4.5；记录console与pageerror。空内容/离线仅作布局状态注入，不冒充服务真实故障。每稿顶部各自显示虚构样例；不显示假同步状态。截图目检后最高checked，native与用户accepted保持未验收。

2026-08-31，用户点名design-before-create并反馈「这个审美很丑」。v0.4视觉方向未通过，保留旧产物。本轮不改生产Dashboard、壳UI或共享交接。

A/B在构图、排版、密度、颜色、几何上不同，不仅换色。全部3条任务、示例公司、状态、期限和步数为虚构排版样例，不代表真实用户任务或已接通数据。每个候选顶部各自声明。第二条无计划和期限，省略对应字段。

纯静态HTML/CSS，无新增可执行行为，TDD不适用；使用浏览器真实渲染、溢出、200%文字、长中文、缺失和对比度检查。后续交互与数据接入继续TDD。

合同沿用技能资产的完整字段，以YAML兼容JSON形式保存。等待用户选择前不接入生产、不安装；不将本项目选择升为通用审美偏好。
