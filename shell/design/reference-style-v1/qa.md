# 参考风格稿 v1（最终渲染 v2）

2026-08-31。状态：specified / generated / rendered / checked；用户accepted未确认。生产候选8未改变。

## 需求与方向

用户提供codex-clipboard-eda27cea-5327-48c2-9db8-26396166fcdb.jpg，并明确只参考风格、内容仍按此前要求。
只借鉴炭黑底、低明度表面、紧凑行距、细分隔线、有限色块和小型图形。内容保持今日概要、实习/工作用途分区、公司或项目与具体工作对象、期限、真实步骤、下一步、原任务入口位置。不复制计时/延时、PVT/KSS、奖励、日程安排或完成任务按钮。
原稿是独立无脚本HTML，不调用API或连接账户；箭头/控件仅位置示意，底栏明确不可点击。

## 数据真实性

index.html顶部两行声明“布局示意 · 非实时数据”“分类、状态、步骤、期限及下一步均为演示”。任务名借用用户已有内容；演示步骤、分类、截止及计数绝不写入生产数据。2进行中+1待开始=3示例条目，已完成0属于明确示例而非真实统计；第2/4步为1完成+1当前+2未做，第3/4步为2完成+1当前+1未做，文字和8分段一致。
missing.html另示只有标题的降级情形：待归类、不提供业务计数/期限/步骤，无假零值、无进行中判定，不画分段条。它不是实时采集结果。

## 验证

- aesthetic-contract.yaml八轴校验ok。
- 依据共享TDD WORKFLOW纯文档/纯样式例外，本轮N/A：没有生产可观察行为变化。以实际Chromium、语义断言、对比度和生产hash核验替代；不把历史128项测试当本轮测试。
- 初次检查脚本在CLI上下文误用全局URL，出现ReferenceError，修正为已有page.url字符串取origin后重跑。该错误不是产品Red。
- v1首图最后一项下一步被底栏遮挡。保留v1三图历史，缩小卡片留白和分区间距、未缩字体，新增任务底部可见性断言，生成v2。
- v2三图由主线逐一目检：普通380x500三项含下一步完整露出；缺失稿不画伪状态；320x500 200%无横向溢出但首屏不能显示完整任务，需纵向滚动。
- 最低实际文字对比度6.0855，>=4.5；零pageerrors；8步骤段/2当前/3完成计数与示例文字一致；无脚本、可点击外部链接或业务按钮。
- 相对manifest-v6核验14项生产/候选8/运行时/测试文件hash全unchanged；未改dashboard、原生壳或读取器。

## 产物

- index.html：信息齐全的明确演示。
- missing.html：没有分类/步骤/期限的明确演示。
- style.css：共享视觉源。
- render.mjs、render-cases.cjs：仅验证脚本，非应用代码。
- ../../output/playwright/reference-style-v2-demo.png
- ../../output/playwright/reference-style-v2-missing.png
- ../../output/playwright/reference-style-v2-200percent.png

原生效果/交互功能/账户同步未验，未构建候选9。独立pre_completion限定静态稿PASS，见supor.md；5项要求独立核验通过。人类判断：深色与紧凑程度是否贴近参考？本次不更改全局审美偏好。
