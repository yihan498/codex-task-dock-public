# 用户确认：A方向与两个产品的边界
日期：2026-08-31。
来源：用户在本对话明确选择“A方案吧”，并说明正在推进两个相似但用途不同的项目。

## 已确认
- 托盘任务浮窗：本对话负责；采用A方向。可呼出、隐藏，帮助用户及时看到手头正在做的具体事情和进度。
- 浏览器workspace项目总览：另一个项目/任务负责。帮助用户了解workspace有哪些项目，以及进行全局管理。
- A的选择只适用于托盘浮窗，不代表网页端采用A，不代表所有细节、实时能力或原生运行已经验收。
- 实习/工作分区、公司＋工作内容＋具体对象、真实步骤、下一步概要常显等既有要求继续适用；没有用户提供的字段仍省略。
- 本次仅记录产品边界，不授权移动目录、拆仓库、修改网页分类、安装或控制Codex任务。

## 实施约束
- 产品层分开设计、分别验收；不得把网页项目目录当作浮窗任务列表，或把网页整页嵌入浮窗视为任务面板完成。
- 可以评估复用只读采集层，但必须区分项目资料、任务运行状态和业务进度；不能将项目文档中的进度当作线程实时步骤。
- A原型仍使用虚构样例；接入时应换成有来源的字段，不能沿用样例时间/状态/数字。
- 独立App Server对active与结构化plan的保真限制仍未解决；“实时”是目标，不是当前完成声明。采集连接正常不等于正在正确追踪任务进度。
- 后续交互和数据接入继续TDD；以真实原生操作与数据更新验收为准。

## 状态与下一步
- 视觉方向：A，用户已选择（direction accepted）。
- B：保留为比较历史，未选用。
- 原生实现、真实数据、安装：尚未整体验收。
- 下一步：围绕A建立独立任务视图的数据与交互合同，再按TDD推进；不继续把网页项目总览当作浮窗的生产UI。
